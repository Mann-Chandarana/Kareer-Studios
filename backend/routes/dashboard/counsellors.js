const express = require('express');
const router = express.Router();

// GET all counsellors
router.get('/', () => {

});

// GET counsllor from id
router.get('/:id', () => {});


// CREATE
router.post('/', () => {

});

router.patch('/:id', () => {});

router.delete('/:id', () => {
    
});

module.exports = router;